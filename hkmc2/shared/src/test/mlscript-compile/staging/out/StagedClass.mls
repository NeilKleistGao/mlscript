#config(noFreeze: true, noModuleCheck: true, deadParamElim: Some(DeadParamElim(debug: false, mono: true)))
import "../StagedClass.mls"
module StagedClass with
    class Baz(val x) with
      ()
    fun bar(b) =
      if b is
        true then StagedClass."Bar$StagedClass"(0)
        else new! StagedClass."Bar$StagedClass".class(1)
    fun baz(b) =
      if b is
        true then Baz(1)
        else new Baz(2)
    fun foo() =
      let {obj_8}
      obj_8 = new! StagedClass."Foo$StagedClass"
      obj_8