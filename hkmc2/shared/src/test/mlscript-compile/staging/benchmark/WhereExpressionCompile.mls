#config(noFreeze: true, noModuleCheck: true)

import "../StagedQuery.mls"

module WhereExpressionCompile with
  fun generate() =
    StagedQuery."generate"("../StagedQuery.mls", "../out/StagedQuery.mls")
