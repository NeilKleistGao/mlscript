#config(liftDefns: None)

import "./Predef.mls"

open annotations { inline }

module NestedFunctionsNoLift with
  @inline fun recursive(n) =
    fun loop(i) =
      if i <= 0 then 0 else loop(i - 1) + 1
    loop(n)
